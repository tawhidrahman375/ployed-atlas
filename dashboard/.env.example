NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Shared secret for the runner to trigger on-demand revalidation after each
# block. Must match REVALIDATE_SECRET in the runner's .env.
REVALIDATE_SECRET=
